#include <stdio.h>

int main() {
    // constant = fixed value that cannot be altered by the program during its execution

    const float PI = 3.14159; // add const before the data type so when we change the value, it'll cause an error.

    printf("%f", PI);

    return 0;;
}