#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main() {
    double A = sqrt(9);
    double B = pow(2, 4);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(3.99);
    double F = abs(-100);
    double G = log(8);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("\n%lf", F);

    return 0;
}