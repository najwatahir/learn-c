#include <stdio.h> /*pre-processor command. to tells the compiler to include the contents of a file*/ // to contains input and output

int main()
{
    printf("hello world");
    printf("\nthere's nothing here");
    return 0; // return the exit status. return 0 if run successfully. return 1 if there's an error.
}
/*escape sequence: \n = newline \t = tab
\"hello\" <- to use a double quote inside string*/